# 能力问题清单（业务层）— chat-panel-resize-min-400 分支调整

> 目的：把本分支（`pr/chat-panel-resize-min-400`，HEAD=`8358e0ebb`）的 9 个提交固化为
> **可独立验证的能力项**。未来任何 rebase / merge / 上游同步后，逐项跑验证即可确认
> 每个调整是否被还原；任何一项失败 = 该调整被改坏，立即修复，不混在其他项里。
>
> 业务层语言 = 零技术词，从用户行为出发。每项必须能回答「用户在哪个界面做什么操作触发」。
> 技术层 claims 见 `.zkge/claims/`，测试映射见 `architecture/score-card.yaml`。

| # | 能力问题（业务层） | 触发入口（界面+操作） | 对应提交 | 风险 |
|---|---|---|---|---|
| C1 | 聊天面板在窗口变窄时仍能保持最小可用宽度，不会被右侧面板挤压到无法阅读 | 拖动窗口宽度 + 拖动右侧面板分隔条 | `59b6140c3` | R2 |
| C2 | 顶部操作栏的次要操作收进「更多」菜单，主操作保持可见，界面不拥挤 | 查看顶部操作栏 + 点击 More 菜单 | `f7639a69d` | R2 |
| C3 | 右侧面板宽度在切换标签（文件/变更/上下文）时保持稳定，不跳动；每个项目的面板宽度独立记忆 | 切换右侧面板标签 + 切换项目 | `ee6591b25` | R2 |
| C4 | 变更列表中点击某条变更后，文件预览不误显示该变更；文件和变更的选择互不干扰 | 文件视图选中文件 → 切到变更视图选变更 → 切回文件 | `98388dae5` | R2 |
| C5 | 文件树滚动到较深位置后，切换标签再回来，滚动位置被恢复 | 滚动文件树 → 切换右侧标签 → 切回 | `7cfb75be2` + `8358e0ebb` | R1 |
| C6 | 变更行的悬停高亮、回滚浮层和状态徽章样式正确显示 | 鼠标悬停变更行 / 点击回滚 | `d8d42210f` | R1 |
| C7 | 侧边栏背景为暖白色调，视觉一致 | 直接观察侧边栏 | `3a1e372fd` | R0 |
| C8 | 文件预览顶部显示面包屑路径，hover 每段显示完整绝对路径；变更概览显示项目名面包屑 | 打开文件预览 / 切到变更概览 | `8358e0ebb` | R1 |
| C9 | 侧边栏和右侧面板的分隔条统一为细线样式，hover 高亮 | 拖动侧边栏/面板分隔条 | `8358e0ebb` | R0 |
| C10 | 文件树的显示/隐藏按钮图标位置正确（在预览区操作组内）；文件树行图标样式统一，选中行高亮 | 查看文件树行图标 + 预览区树开关按钮 | `ee6591b25` + `d8d42210f` | R1 |
| C11 | 改动面板行结构（内边距/图标）、整行悬停高亮、revert 按钮悬停浮现、状态徽章、选中行持久高亮 | 打开右侧面板变更视图，查看行 hover / revert / 徽章 / 选中态 | `d8d42210f` | R1 |
| C12 | 手动调整过的文件树宽度在关闭预览再打开后保持，不回到均分；面板过窄时树自动隐藏只留预览 | 拖宽文件树 → 打开文件 → 关闭预览 → 再打开文件 | `331462c77` + `ee6591b25` | R2 |
| C13 | 面板内部分栏时预览区左缘去重复边框（无双线）；文件树拖拽条为细线样式 | 查看分栏预览左缘 + 文件树左缘拖拽条 | `8358e0ebb` | R1 |

## 还原验证方法（每项怎么确认「被还原」）

每项 = 1 个 claim（技术层，带 oracle）+ 1 个测试或显式验证步骤。执行顺序：

1. 代码在 `8358e0ebb` 时跑 `workspace-layout.test.ts` + `workspace-tree-scroll-restore.test.tsx` + `workspace-changes-errors.test.tsx` 等 → 全绿 = 基线
2. rebase/merge 后重跑同一组测试 → 任何 FAIL 即定位到对应 C#，单独修复
3. 静态检查：`grep` 关键符号确认实现存在（见各 claim 的 oracle）
4. UI 手测：按「触发入口」列操作确认视觉效果

## 已确认的改坏教训（本次 rebase 中发生过）

- **C3/C8**：rebase 到上游 `5190740b0` 时，分支的 `workspacePanelSession`/`recentPaths` 独立持久化被上游 `workspaceTreeMemory` 覆盖丢失 → 正是需要本清单防止的
- **C1**：`workspacePanelAvailableWidth` 定义在冲突解决时被删 → 运行时 `Can't find variable` 崩溃
